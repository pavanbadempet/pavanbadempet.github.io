name = "portfolio-ai-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Set secret in Cloudflare: wrangler secret put OPENAI_API_KEY
[vars]
# Optional: lock CORS to your GitHub Pages origin (comma-separated). Default in code is *.
# ALLOWED_ORIGIN = "https://pavanbadempet.github.io"
